# SETTLEM ACADEMY — FINAL PACKAGE

This package combines the latest frontend (including Module 04 / 20-question test support) and the latest backend.

## Frontend
Open through a web server, not `file://` when testing API features.

## Backend
See `backend/README.md` and `frontend/FINAL_PROJECT_CHECKLIST.md`.

## Important
The frontend is ready for deployment, but live API/database behavior must be verified after the backend is deployed and PostgreSQL is connected.
